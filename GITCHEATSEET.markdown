GIT Cheatseeht
==============

git add .
Add modified and new files

Undo add
git rm -r --cached .

git commit
commit to local repository

git gommit origin master
commit to github

reset to last commit
git reset --hard HEAD